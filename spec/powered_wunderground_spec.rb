require 'spec_helper'

describe PoweredWunderground do
  it 'has a version number' do
    expect(PoweredWunderground::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
